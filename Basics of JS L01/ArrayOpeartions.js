// // Push and Unshift
// let fruits = ["Apple" , "Mango"];
// fruits.push("Banana"); //puhes at the end of array
// fruits.unshift("Guava"); //pushes in the front of the array
// fruits.pop(); //removes from the end
// fruits.shift(); //removes from the front

// //Splice method is used to remove any method from any specific location but when we keep no. of elements as 0, it is capable of adding an element at that position (Synatx: arr.splice(index, no_of_elements))
// fruits.splice(1, 0, "Kiwi"); //Insertion at an index

// let items = [1,2,3,4,5,6];
// //extraction of 3 middle elements
// console.log(items.slice(1,4));

// //Sort array
// fruits.sort().reverse();